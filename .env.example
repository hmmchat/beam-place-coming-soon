# Copy to .env on the server (never commit .env).
# Google Apps Script web app URL (handles waitlist and dare drops).
GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
